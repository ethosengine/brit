# etc/plan — Sebastian's upstream parity plans

Reconciled migration plans imported from upstream gitoxide (currently: `gix-error` migration from `thiserror`, SHA-256 object-format transition). Checkboxes in these files reflect the current checkout's state, not upstream issue history.

Work against these plans belongs on `gix-brit`. When a batch closes, repackage as Sebastian-spec commits off `gix-main` for upstream PR — not via automation.

See root `CLAUDE.md` for branch discipline and agent roster.
