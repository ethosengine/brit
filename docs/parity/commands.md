# Parity Matrix — git ↔ gix

Top-level command-level status. Per-flag status lives inside each `tests/journey/parity/<cmd>.sh`. See root `CLAUDE.md` for loop structure.

Legend:
- `absent` — no gix implementation
- `partial` — some flags / modes implemented, not all; has a `tests/journey/parity/<cmd>.sh` with TODO rows
- `present` — full parity claimed AND verified by `@gix-steward`
- `deferred` — hard system constraint only; operator-approved

Deferred flag-level rows and compat-only rows live in [SHORTCOMINGS.md](SHORTCOMINGS.md), regenerated from `tests/journey/parity/*.sh` by `etc/parity/shortcomings.sh`.

## Porcelain

| git | gix | Status | Parity file | Notes |
|---|---|---|---|---|
| push | gix push | present | [push.sh](../../tests/journey/parity/push.sh) | 68 green `it` blocks across 48 sections. Deferrals: sha256 remotes, actual lease-mismatch / force-if-includes enforcement, non-default receive-pack programs, actual push-option transmission, push-cert generation, submodule-push, and live-TCP `-4`/`-6` family selection — documented as prose in `tests/journey/parity/push.sh` header; ledger retrofit via `compat_effect`/`shortcoming` markers pending. |
| pull | gix pull | present | [pull.sh](../../tests/journey/parity/pull.sh) | 90 green `it`-bearing assertions across 65 sections covering the full git-pull flag surface from vendor/git/Documentation/git-pull.adoc + the inherited merge-options.adoc + fetch-options.adoc surface, plus vendor/git/builtin/pull.c options[] at L870..1009 — meta (--help / --bogus-flag / outside-of-repo), 4 synopsis forms (bare-no-upstream / `<remote>` / `<remote> <refspec>` / `<remote> <bad-revspec>`), shared verbosity (-v / --verbose / -q / --quiet / --progress / --no-progress / --recurse-submodules[=mode] / --no-recurse-submodules), merging options (-r/--rebase[=true|false|merges|interactive] / --no-rebase, -n/--no-stat / --stat / --summary / --no-summary / --compact-summary, --log[=n] / --no-log, --signoff / --no-signoff, --squash / --no-squash, --commit / --no-commit, --edit / --no-edit / --cleanup, --ff / --no-ff / --ff-only, --verify / --no-verify, --verify-signatures / --no-verify-signatures, --autostash / --no-autostash, -s/--strategy / -X/--strategy-option, -S/--gpg-sign[=key-id] / --no-gpg-sign, --allow-unrelated-histories), fetching options (--all, -a/--append, --upload-pack, -f/--force, -t/--tags, -p/--prune, -j/--jobs, --dry-run, -k/--keep, --depth / --shallow-since / --shallow-exclude / --deepen / --unshallow / --update-shallow, --refmap, -o/--server-option, -4/--ipv4 / -6/--ipv6, --negotiation-tip, --show-forced-updates / --no-show-forced-updates, --set-upstream). Real implementations: top-level porcelain `Pull(pull::Platform)` clap surface mirroring git's union of fetch + merge flags (NOT a mechanical flatten — git pull's options[] is a union of the subsets each sub-command actually accepts, e.g. fetch's --multiple is excluded), bytes-mode bare-no-upstream gate in `gitoxide_core::repository::pull::porcelain` ("There is no tracking information for the current branch." 8-line stanza on stderr + exit 1, NOT 128 — pull's die() routes through advise_die and uses git's standard exit-1 default), --rebase[=value]/--no-rebase variant detection via `rebase_active()` (mirrors git's quirk where opt_rebase pre-loads remote_state via get_rebase_fork_point, so for_each_remote in die_no_merge_candidates resolves "origin" instead of "<remote>" and the action verb flips from "merge with" to "rebase against"), --dry-run short-circuit (vendor/git/builtin/pull.c:1086 mirror) — silent exit-0 even with no upstream because cmd_pull returns 0 before reaching die_no_merge_candidates, drop the `-e` short on --edit (git pull does NOT take `-e`; vendor/git/builtin/pull.c OPT_PASSTHRU(0, "edit", ...)). PARSE_OPT_OPTARG mirroring: `-r/--rebase`, `--log`, `--signoff`, `-S/--gpg-sign`, `-j/--jobs` use `require_equals = true` so `-r dev` / `--log dev` / etc. parse as flag + positional, matching git's "must be stuck without a space" semantic. Bytes-mode rows: most flag-only rows close as `expect_parity bytes` against `small-repo-in-sandbox` because both binaries hit the bare-no-upstream gate and emit byte-exact stanza output. Bytes-mode synopsis row: bare-no-upstream. Effect-mode rows: --help, --bogus-flag, outside-of-repo (exit-code-only contract; output text diverges between man-delegate and clap), -e (both 129 with different "unknown switch"/"unexpected argument" wording), --unshallow (both 1 with different "complete repository"/no-upstream wording). Synopsis happy-path rows close as `compat_effect "deferred until pull driver lands"` against the new `pull-fixture-with-tracking` helper (bare upstream + clone with origin/main tracking — both binaries exit 0). Deferrals: see [SHORTCOMINGS.md#pull](SHORTCOMINGS.md#pull) — 1 row closes as `shortcoming "system git 2.47.3 lacks --compact-summary; vendor/git v2.54.0 has it"` (version skew is a hard system constraint, lifts when the test runtime upgrades), 3 rows close as `shortcoming "...deferred until pull driver wires fetch composition"` (-a/--append where `git fetch --append` opens .git/FETCH_HEAD before the merge-candidates check yielding exit 128 vs gix's stub at exit 1; <bad-revspec> where exit-1 emerges from the fetch step). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| fetch | gix fetch | present | [fetch.sh](../../tests/journey/parity/fetch.sh) | 89 green `it` blocks across 61 sections. Deferrals: see [SHORTCOMINGS.md#fetch](SHORTCOMINGS.md#fetch). sha256 blocker: gix cannot open sha256 remotes yet (every row `sha1-only`). |
| clone | gix clone | present | [clone.sh](../../tests/journey/parity/clone.sh) | 1 top-level section header + 48 flag-scoped title sections; expect_parity + expect_run assertions, sha256 skipped via only_for_hash. Deferrals: see [SHORTCOMINGS.md#clone](SHORTCOMINGS.md#clone). sha256 blocker: gix cannot open sha256 remotes yet (every row `sha1-only`). |
| merge | gix merge | present | [merge.sh](../../tests/journey/parity/merge.sh) | 60 green `it` blocks + 1 shortcoming covering the full git-merge porcelain surface from vendor/git/Documentation/git-merge.adoc + merge-options.adoc + rerere-options.adoc + signoff-option.adoc + vendor/git/builtin/merge.c (builtin_merge_options at L261..339) — meta (--help/--bogus-flag/outside-repo), synopsis forms (`<commit>` ancestor / fast-forward / 3-way / octopus / `<bad-revspec>` / bare-no-upstream), diffstat (-n/--stat/--no-stat/--summary/--no-summary/--compact-summary), shortlog (--log[=n]/--no-log), squash/commit/edit/cleanup, fast-forward (--ff/--no-ff/--ff-only), rerere (--rerere-autoupdate/--no-rerere-autoupdate), signature verification (--verify-signatures/--no-verify-signatures), strategy (-s/--strategy/-X/--strategy-option), message (-m/--message/-F/--file/--into-name), verbosity (-v/-q), in-progress transitions (--abort/--quit/--continue), histories/progress, GPG signing (-S/--gpg-sign/--no-gpg-sign), autostash, overwrite-ignore, signoff, hooks (--verify/--no-verify). Real implementations: top-level porcelain `Merge(merge::Platform { cmd: Option<SubCommands>, commits, ...flags })` reshape (replaces the bare-Subcommand form so `gix merge <commit>` and the flag surface parse cleanly; existing `file`/`tree`/`commit` plumbing subcommands kept as escape hatches under the optional cmdmode), bytes-mode in-progress transition gates in `gitoxide_core::repository::merge::porcelain` ("fatal: There is no merge to abort (MERGE_HEAD missing)." + 128 for --abort, silent exit-0 for --quit, "fatal: There is no merge in progress (MERGE_HEAD missing)." + 128 for --continue), bytes-mode bare-no-upstream gate ("fatal: No remote for the current branch." + 128 when commits is empty), bytes-mode bad-revspec gate via `Repository::rev_parse_single` per positional ("merge: <ref> - not something we can merge" + 1 on first unresolvable). PARSE_OPT_OPTARG mirroring: `--log` and `-S/--gpg-sign` use `require_equals = true` so `--log dev` / `-S dev` parse as flag + positional, matching git's "must be stuck without a space" semantic. Bytes-mode rows: bare-no-upstream, bad-revspec, --abort, --quit, --continue. Deferrals: see [SHORTCOMINGS.md#merge](SHORTCOMINGS.md#merge) — 47 flag rows close as `compat_effect "deferred until merge driver lands"` (every flag whose semantics depend on the unimplemented merge driver: fast-forward / 3-way / octopus paths, AUTO_MERGE / MERGE_HEAD / MERGE_MSG ref writes, conflict-marker emission); 1 row closes as `shortcoming "system git 2.47.3 lacks --compact-summary; vendor/git v2.54.0 has it"` (version skew between the test runtime's git and vendor/git is a hard system constraint and lifts when the test runtime upgrades). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| rebase | gix rebase | present | [rebase.sh](../../tests/journey/parity/rebase.sh) | 77 green `it` blocks across 77 sections covering the full git-rebase flag surface from vendor/git/Documentation/git-rebase.adoc + vendor/git/builtin/rebase.c (cmd_rebase, builtin_rebase_options[] at L1120..1247) — meta (--help / --bogus-flag / outside-of-repo / bare-no-upstream), synopsis forms (`<upstream>` already-up-to-date / fast-forward / `<upstream> <branch>` / `<bad-revspec>`), starting-point (--onto / --keep-base / --root), in-progress transitions (--continue / --skip / --abort / --quit / --edit-todo / --show-current-patch), backend selection (--apply / --merge / -m / --interactive / -i), empty/cherry-pick (--empty=drop|keep|stop / --keep-empty / --no-keep-empty / --reapply-cherry-picks / --no-reapply-cherry-picks / --allow-empty-message), strategy (--strategy / -s / --strategy-option / -X / --rebase-merges[=mode] / --no-rebase-merges / -r), force/fork-point (--force-rebase / -f / --no-ff / --fork-point / --no-fork-point), interactive companions (--exec / -x / --autosquash / --no-autosquash / --reschedule-failed-exec / --no-reschedule-failed-exec / --update-refs / --no-update-refs), verbosity (--quiet / -q / --verbose / -v / --stat / --no-stat / -n), hooks (--no-verify / --verify), apply-backend passthroughs (-C / --ignore-whitespace / --whitespace), trailers/signoff/authorship (--trailer / --signoff / --committer-date-is-author-date / --reset-author-date / --ignore-date), rerere (--rerere-autoupdate / --no-rerere-autoupdate), autostash (--autostash / --no-autostash), GPG signing (--gpg-sign[=key-id] / -S / --no-gpg-sign). Real implementations: top-level porcelain `Rebase(rebase::Platform)` clap surface mirroring `builtin_rebase_options[]` with positional `[<upstream> [<branch>]]`; bare-no-upstream stanza in `gitoxide_core::repository::rebase::porcelain` (git's verbatim 9-line "There is no tracking information for the current branch." stanza on **stdout** + exit 1, NOT stderr — see vendor/git/builtin/rebase.c:1058 `error_on_missing_default_upstream` calls `printf`, not `fprintf(stderr)`; two variants split on detached-HEAD ("You are not currently on a branch.") vs. on-a-branch with the trailing `git branch --set-upstream-to=<remote>/<branch>` hint); bad-revspec gate that rev-parses `<upstream>` via `repo.rev_parse_single` and emits "fatal: invalid upstream '<ref>'" + exit 128 on parse failure; cmdmode precondition gate emitting "fatal: no rebase in progress" + exit 128 unconditionally for --continue/--skip/--abort/--quit/--edit-todo/--show-current-patch (gix has no `.git/rebase-merge/` / `.git/rebase-apply/` state-dir lookup yet — every test fixture is clean, so the no-rebase-in-progress branch is the only path closed today). PARSE_OPT_OPTARG mirroring: `-r/--rebase-merges` and `-S/--gpg-sign` use `require_equals = true` so `-r dev` / `-S dev` parse as flag + positional, matching git's "must be stuck without a space" semantic. Bytes-mode rows: bare-no-upstream, <bad-revspec>, --continue, --skip, --abort, --quit, --edit-todo, --show-current-patch. Effect-mode rows: --help, --bogus-flag, (outside-of-repo). Deferrals: see [SHORTCOMINGS.md#rebase](SHORTCOMINGS.md#rebase) — 66 rows close as `compat_effect "deferred until rebase driver lands"` (every flag whose semantics depend on the unimplemented rebase driver: revision-walk + cherry-pick replay; apply vs. merge backend; --interactive todo-list emission/edit/replay; --rebase-merges branching-structure preservation; AUTO_MERGE / REBASE_HEAD / `.git/rebase-merge/` / `.git/rebase-apply/` ref+state writes); 1 row closes as `shortcoming "system git 2.47.3 lacks --trailer; vendor/git v2.54.0 has it"` (version skew between the test runtime's git and vendor/git is a hard system constraint and lifts when the test runtime upgrades). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). Stub crate `gix-rebase` exists but is not wired (porcelain layer is the parity surface). |
| reset | gix reset | present | [reset.sh](../../tests/journey/parity/reset.sh) | 43 green `it` blocks across 43 sections (40 OK runs total: 38 sha1-side + 2 sha256-side from the two dual rows) covering the full git-reset flag surface from vendor/git/Documentation/git-reset.adoc + vendor/git/builtin/reset.c:350..382 — meta (--help / --bogus-flag / outside-of-repo), synopsis (default mixed / HEAD / HEAD~1 / unborn-HEAD / `<bad-revspec>`), reset modes (--mixed / --soft / --hard / --merge / --keep + last-wins `--soft --hard`), mode-vs-paths interactions (`--soft -- a` / `--mixed -- a`), verbosity / refresh (-q / --quiet / --refresh / --no-refresh), recurse-submodules (`--recurse-submodules` / `=yes` / `--no-recurse-submodules`), patch (--patch / -p), intent-to-add (-N / --intent-to-add), pathspec sources (--pathspec-from-file / --pathspec-file-nul + the sole-`--pathspec-file-nul` precondition), pathspec positionals (HEAD -- a / HEAD a / -- a), and precondition gates (bare-repo / -N --hard / --patch --hard / --patch --pathspec-from-file / --pathspec-from-file -- a). Mode breakdown: 9 `expect_parity bytes` (outside-of-repo, `<bad-revspec>`, `--soft -- a`, `--pathspec-file-nul`, `(in bare repo)`, `-N --hard`, `--patch --hard`, `--patch --pathspec-from-file=spec.txt`, `--pathspec-from-file=spec.txt -- a` — the outside-of-repo row is dual); 2 `expect_parity effect` (--help, --bogus-flag); 27 `compat_effect "deferred until reset driver lands"`; 5 `shortcoming "system git 2.47.3 lacks <flag>; vendor/git v2.54.0 has it"` (the patch-context rows --auto-advance / --no-auto-advance / --unified=3 / -U 5 / --inter-hunk-context=2 — version skew between the test runtime's git and vendor/git). Real implementations: `Reset(reset::Platform)` clap surface mirroring `vendor/git/builtin/reset.c:350..382`; porcelain placeholder in `gitoxide_core::repository::reset` wiring the bad-revspec gate (`fatal: ambiguous argument...` + 128 for the single-positional / non-unborn case) plus the C-mirror precondition matrix (`--pathspec-file-nul` requires `--pathspec-from-file`; `--pathspec-from-file` mutually exclusive with `--patch` and with explicit `<pathspec>`; `--patch` mutually exclusive with `--{hard,mixed,soft}`; non-mixed mode + paths dies; default-mixed in bare-repo dies; `-N` requires `--mixed`); each gate emits the verbatim git wording so the corresponding rows close as `expect_parity bytes`. The remaining compat_effect rows are deferred until the real reset driver implements the three-way unpack matrix from `vendor/git/Documentation/git-reset.adoc:386..506`. sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| commit | gix commit | present | [commit.sh](../../tests/journey/parity/commit.sh) | 62 green `it` blocks across 43 flag-scoped sections covering the full git-commit flag surface from vendor/git/Documentation/git-commit.adoc OPTIONS: meta (--help, --bogus-flag, outside-repo, nothing-to-commit), message sources (-m, -F + stdin, -t/--template, -C/--reuse-message, -c/--reedit-message, --squash, --fixup [plain/amend:/reword:]), staging modes (-a/-i/-o/-p/--pathspec/--pathspec-from-file/--pathspec-file-nul + `-- <pathspec>` terminator), amend/rewrite (--amend [--no-edit and new -m], --reset-author precondition gate, --no-post-rewrite), allow-empty (--allow-empty, --allow-empty-message), hooks (-n/--no-verify, --verify), authorship (--author with explicit ident, --date with gix-date::parse), trailers (-s/--signoff, --no-signoff, --trailer), cleanup (strip/whitespace/verbatim/scissors/default/bogus → exit-128), editor (-e/--edit, --no-edit), output modes (--dry-run, --short, --branch, --porcelain, --long, -z/--null, --status/--no-status, -v/--verbose [-v + -vv], -q/--quiet, -u/--untracked-files [no/normal/all]), signing (-S/--gpg-sign emits git-compat "gpg failed to sign" + exit 128, --no-gpg-sign). Real implementations: porcelain `Commit(commit::Platform { cmd: Option<Subcommands>, ...flags })` reshape (replaces the bare-Subcommand form so `gix commit` accepts the flag surface), CreateOptions struct in gitoxide-core/src/repository/commit.rs threading 22 fields through the create() entry point, message composition pipeline (-m join + -C/-c full-message load + --squash/--fixup subject prefixing + -F path/stdin append + --trailer per-line append), --reset-author / --include / --only / --cleanup precondition gates that mirror git's exit-128 wording verbatim, --gpg-sign exit-128 stanza ("error: gpg failed to sign the data" + "fatal: failed to write commit object"), --amend write_object + edit_reference path (HEAD's parents become new commit's parents; PreviousValue::MustExistAndMatch(HEAD) under the ref update; original author preserved unless --author/--date override), commit_as path for non-amend rows (parents = [HEAD]), --author parsed via gix::actor::IdentityRef::from_bytes, --date parsed via gix::date::parse. Index→tree primitive is not yet wired in gix; the corresponding rows (-a, -i, -o, -p, --dry-run, --short, --branch, --porcelain, --long, -z, --status, -v, -u, --untracked-files, --pathspec, `-- <pathspec>`) close via clean-fixture exit-1 parity (both binaries exit 1 on a clean small-repo-in-sandbox: git "nothing to commit, working tree clean"; gix porcelain bail with the explicit index→tree-pending guard). Bytes parity on the dirty-path output rides the same primitive. sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: `--help`, `(outside a repository)`. |
| status | gix status | present | [status.sh](../../tests/journey/parity/status.sh) | 51 green `it` blocks across 26 sections (short/long, branch/show-stash, porcelain=v1/v2, verbosity, untracked-files modes, ignore-submodules modes, ignored modes, -z, column, ahead-behind, renames, find-renames, pathspec). Rows flip to `sha1-only` because gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`). Deferrals (compat-accept rows: effect-mode parity holds, byte-output pending): `--show-stash`, `-v/--verbose`, `--ignore-submodules=<mode>`, `--column/--no-column`, `--ahead-behind/--no-ahead-behind`, `--renames/--no-renames`, `--find-renames[=<n>]`, porcelain=v2 headers for branch/stash/detached-HEAD/initial-repo — documented as prose in `tests/journey/parity/status.sh` headers; ledger retrofit via `compat_effect` markers pending. |
| log | gix log | present | [log.sh](../../tests/journey/parity/log.sh) | 220 green `it` blocks covering the full user-visible git-log flag surface (rev-list-options + diff-options + pretty-options + git-log proper, minus rev-list-only `ifdef::git-rev-list[]` entries and `--max-depth` / `--maximal-only` which git-log itself rejects at runtime despite their unconditional adoc entries). Real implementations: revspec parsing (Include/Range/Merge/IncludeOnlyParents/etc via gix_revision::Spec), pseudo-refs (--all/--branches/--tags/--remotes), parent-count predicate (--merges / --no-merges / --min-parents / --max-parents), iterator adapters (-n/--max-count/--skip/-N), log-specific exit-code remaps (--bogus-flag→128, unborn HEAD → byte-exact git wording + 128, ambiguous-arg → byte-exact 3-line stanza + 128). Remaining flags are clap-accepted and tracked in [SHORTCOMINGS.md](SHORTCOMINGS.md) as compat rows — pretty/format, decorate, diff output, date formatting, pickaxe (-G/-S), cherry/left-right, reflog walk (-g), history simplification, whitespace-diff, diff algorithms (--minimal/--patience/--histogram/--anchored), diff-filter, word-diff, color-moved/color-words, prefix/output, rev-list companions (--reflog/--stdin/--ignore-missing/--since-as-filter/--bisect/--relative-date/--exclude-hidden), combined-diff (--dd/--combined-all-paths/--output-indicator-*/-t), rename/copy (-B/-C/-l/--no-renames/--rename-empty), path control (-O/-R/--skip-to/--rotate-to/--ignore-submodules/--default-prefix/--line-prefix/--ita-invisible-in-index), deprecated notes aliases (--show-notes/--standard-notes), pretty companions (--encoding/--expand-tabs), `-P` / `--basic-regexp` regex modes. One legitimate shortcoming: --merge (git exits 128 without merge state; gix has no precondition check). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: `--help`, `(outside a repository)`. |
| diff | gix diff | present | [diff.sh](../../tests/journey/parity/diff.sh) | 104 green `it` blocks across 106 sections covering the git-diff flag surface from vendor/git/Documentation/git-diff.adoc + diff-options.adoc + builtin/diff.c — meta (--help/--bogus-flag/outside-repo), 7 synopsis forms (bare/--cached/<commit>/<commit> <commit>/A..B/A...B/<blob> <blob>/--no-index/--merge-base), output formats (-p/--raw/--name-only/--name-status/--stat/--shortstat/--numstat/--dirstat/--summary/-z), output controls (-U/--output/--abbrev/--binary/--full-index/--{src,dst,line}-prefix), color/word-diff (--color/--color-moved/--color-moved-ws/--word-diff/--color-words), algorithm (--minimal/--patience/--histogram/--anchored/--diff-algorithm/--indent-heuristic), whitespace (-a/--ignore-{cr,space,blank,matching}/-b/-w/--ws-error-highlight/--check/--inter-hunk-context/-W), detection (--no-renames/-B/-M/-C/--diff-filter/-D), pickaxe (-S/-G/--find-object/--pickaxe-{all,regex}), path control (-R/--relative/--skip-to/--rotate-to), submodule/textconv/ext-diff, exit-code, merge-stage selection (-1/-2/-3/-0), combined-diff. Real implementations: top-level porcelain `Diff(diff::Platform { cmd: Option<SubCommands>, args, paths, no_index, ... })` reshape (replaces the plumbing-only Tree/File form so `gix diff` accepts the bare flag surface), bare-form dispatch in `src/plumbing/main.rs` that routes to `gitoxide_core::repository::diff::porcelain` (worktree-vs-index for 0 args; rev-parse with git-exact "ambiguous argument" stanza on failure for 1-2 args; range splitting for `A..B` / `A...B` via parse_range with merge-base resolution; blob-vs-blob detection via Kind::Blob check; trailing-pathspec collapse so `diff HEAD~1 HEAD b` exits 0), `diff::no_index` short-circuit before repo discovery (reads two files via gix::path + std::fs and returns 0/1 by byte-equality), explicit `gix::discover::upwards` outside-repo gate (exit 129 with git's "warning: Not a git repository..."). Deferrals: see [SHORTCOMINGS.md#diff](SHORTCOMINGS.md#diff) — bulk of rows close as `compat_effect` because gix has no patch / raw / stat / dirstat / summary / word-diff / color-moved / pickaxe renderer wired into `gix diff` yet (shared deferral phrase: "deferred until renderer lands"); `<merge> <merge>^@` and `--combined-all-paths` close as `shortcoming()` (gix-rev lacks `^@` parent-set syntax + combined-diff renderer unimplemented). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository), --no-index. |
| show | gix show | present | [show.sh](../../tests/journey/parity/show.sh) | 132 green OK assertions across 130 sections (128 sha1-only + 2 dual; sha256 reruns of the 2 dual rows account for the 132 vs 130 delta) covering the full git-show flag surface from vendor/git/Documentation/git-show.adoc + vendor/git/builtin/log.c::cmd_show (entry at vendor/git/builtin/log.c:657, options inherited via cmd_log_init from pretty-options.adoc + diff-options.adoc + diff-generate-patch.adoc) — meta (--help / --bogus-flag / outside-of-repo), synopsis (default-HEAD / unborn-HEAD / `<ref>` / `<sha>` / `<unknown-ref>` / `<tag>` / `<tree>` / `<blob>` / multi-object), pretty-options (--pretty[=fmt] / --format / --abbrev-commit / --no-abbrev-commit / --oneline / --encoding / --expand-tabs / --no-expand-tabs / --notes[=ref] / --no-notes / --show-notes-by-default / --show-signature), diff-options + diff-generate-patch (-p / --patch / -s / --no-patch / -U / --unified / --output / --output-indicator-{new,old,context} / --raw / --patch-with-raw / --indent-heuristic / --no-indent-heuristic / --minimal / --patience / --histogram / --anchored / --diff-algorithm / --stat / --numstat / --shortstat / --compact-summary / --dirstat / --cumulative / --summary / --patch-with-stat / -z / --name-only / --name-status / --submodule / --color / --no-color / --color-moved / --no-color-moved / --color-moved-ws / --no-color-moved-ws / --word-diff / --word-diff-regex / --color-words / --no-renames / --rename-empty / --no-rename-empty / --check / --ws-error-highlight / --full-index / --binary / --abbrev / -B / -M / --find-renames / -C / --find-copies / --find-copies-harder / -D / --irreversible-delete / -l / --diff-filter / -S / -G / --find-object / --pickaxe-all / --pickaxe-regex / -O / --skip-to / --rotate-to / -R / --relative / --no-relative / -a / --text / --ignore-cr-at-eol / --ignore-space-at-eol / -b / --ignore-space-change / -w / --ignore-all-space / --ignore-blank-lines / --ignore-matching-lines / -I / --inter-hunk-context / -W / --function-context / --exit-code / --quiet / --ext-diff / --no-ext-diff / --textconv / --no-textconv / --ignore-submodules / --src-prefix / --dst-prefix / --no-prefix / --default-prefix / --line-prefix / --ita-invisible-in-index), and the merge-diff family (--diff-merges / --no-diff-merges / -c / --cc / -m / --combined-all-paths / --remerge-diff / -t / --dd). Real implementations: top-level porcelain `Show(show::Platform)` clap surface mirroring git-show.adoc with `[<object>...]` positional, UnknownArgument exit-code remap extended to include "show" alongside "log" so `--bogus-flag` returns 128 (matching git's keep-unknown-opt + die path in builtin/log.c), unborn-HEAD precondition gate emitting verbatim "fatal: your current branch '<name>' does not have any commits yet" + exit 128 on stderr (parity with vendor/git/builtin/log.c → cmd_log_walk → revision.c::die), bad-revspec gate emitting verbatim 3-line "fatal: ambiguous argument..." stanza + exit 128 (parity with vendor/git/revision.c::handle_revision_arg). `git show` accepts only the short `-O <file>` form for orderfile (vendor/git/diff.c wires it as `OPT_FILENAME('O', NULL, ...)` — no long alias), so gix's clap surface drops `long = "orderfile"`. Bytes-mode rows: unborn-HEAD and `<unknown-ref>` (both byte-exact precondition error stanzas). Deferrals: see [SHORTCOMINGS.md#show](SHORTCOMINGS.md#show) — bulk of rows close as `compat_effect "deferred until show driver lands"` because gix has no per-object dispatch yet (blob → verbatim bytes; tag → "tag <name>\n" framing + recursive tagged-object resolution; tree → "tree <name>\n\n" + ls-tree --name-only emission; commit → pretty-format + diff with `dense-combined` as the default for merges per git-show.adoc:54). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| blame | gix blame | present | [blame.sh](../../tests/journey/parity/blame.sh) | 50 green `it` blocks across 51 flag-scoped sections covering the full git-blame flag surface from vendor/git/Documentation/blame-options.adoc + git-blame.adoc + vendor/git/builtin/blame.c (cmd_blame option table at L951..986) — meta (--help, --bogus-flag, bare-no-file, outside-of-repo), basic invocation (default, missing-file, HEAD <file>, -- <file>), output modes (-p/--porcelain, --line-porcelain, --incremental, -c, -t, -l, -s, -e/--show-email, -f/--show-name, -n/--show-number), line-range filter (-L A,B + multi -L A,B -L C,D + the `+N` offset form), stats/debug (--show-stats, --score-debug), abbrev (--abbrev=<n>), date/since/reverse (--date=<fmt>, --since=<date>, --reverse <range>, --first-parent), boundary (-b, --root), move/copy detection (-M, -M=<num>, -C, -C -C, -C -C -C), ignore-rev / ignore-revs-file, whitespace (-w), --diff-algorithm=<algo>, --minimal, color (--color-lines, --color-by-age), progress (--progress, --no-progress, --progress --porcelain incompatibility bytes-exact), contents/encoding (--contents <file>, --encoding=<enc>), -S graft. Real implementations: top-level Blame Clap reshape from `<file>` positional to `[REV...] [--] <file>` mirroring git's synopsis (rev-parse via `repo.rev_parse_single` -> `peel_to_commit` for the suspect, with git's exact 3-line "ambiguous argument" stanza + exit 128 on parse failure), gitoxide_core::repository::blame::blame_file FileMissing-error remap to "fatal: no such path '<file>' in HEAD" + exit 128 (parity with builtin/blame.c's fake_working_tree_commit -> die path), `--progress` + `--porcelain`/`--incremental` precondition gate emitting the verbatim "fatal: --progress can't be used with --incremental or porcelain formats" + exit 128 (parity with builtin/blame.c:1043-1048), `-s/--statistics` rename to `--show-stats` (matching git's actual perf-stats flag) so `-s` is free for OUTPUT_NO_AUTHOR. Bytes-mode rows: `<missing-file>` and `--progress --porcelain` (both byte-exact precondition error stanzas). Deferrals: see [SHORTCOMINGS.md#blame](SHORTCOMINGS.md#blame) — bulk of rows close as `compat_effect` because gix has no porcelain / line-porcelain / incremental / annotate-compat / show-email / show-name / show-number / boundary-blanking / abbrev-tunable / date-format / encoding-transcoding / color / move-copy / ignore-rev / whitespace-ignore / diff-algorithm-threading / contents / -S graft renderer wired into `gix blame` yet (shared deferral phrase: "renderer deferred"); `-L <start>,` open-ended, `-L :<funcname>`, and `-L /<regex>/,<end>` close as `shortcoming()` (AsRange parser at src/shared.rs only models `<start>,<end>`; userdiff/funcname-pattern infra and regex-anchored range parsing not yet in gix-diff). `-M[<num>]` / `-C[<num>]` use `require_equals = true` so bare `-M b` keeps `b` as the file (mirrors git's PARSE_OPT_OPTARG `-M` + positional `b` semantics); sticky `-M30` is rejected by gix's clap surface — tests use `-M=30` (also accepted by git). sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| add | gix add | present | [add.sh](../../tests/journey/parity/add.sh) | 49 green `it` blocks across 54 sections (51 OK runs total: 49 sha1-side + 2 sha256-side from the two dual rows) covering the full git-add flag surface from vendor/git/Documentation/git-add.adoc + vendor/git/builtin/add.c:253..283 — meta (--help / --bogus-flag / outside-of-repo), empty-pathspec ("Nothing specified, nothing added." stanza), pathspec positionals (`a` / `new-file` / `.` / `-- a` / `missing-file`), verbosity / dry-run (-n / --dry-run / -v / --verbose), force/sparse/update/all/no-all (-f / --force / -u / --update / -A / --all / --no-all / --ignore-removal / --no-ignore-removal / --sparse), intent-to-add (-N / --intent-to-add), renormalize, refresh / ignore-errors, chmod (--chmod=+x / --chmod=-x), pathspec sources (--pathspec-from-file / --pathspec-file-nul), interactive / patch / edit (-i / --interactive / -p / --patch / -e / --edit / --auto-advance --patch / -U 3 --patch / --unified=3 --patch / --inter-hunk-context=2 --patch), ignore-missing companion (--dry-run --ignore-missing), and precondition gates (-A -u / --ignore-missing / --chmod=foo / --pathspec-from-file -- a / --pathspec-file-nul / --pathspec-from-file --interactive / --dry-run --interactive / --pathspec-from-file --edit / --unified=3 / --inter-hunk-context=2 / --no-auto-advance). Mode breakdown: 11 `expect_parity bytes` (outside-of-repo dual + 10 precondition gates), 2 `expect_parity effect` (--help dual, --bogus-flag), 36 `compat_effect "deferred until add driver lands"`, 7 `shortcoming "system git 2.47.3 lacks <flag>; vendor/git v2.54.0 has it"` (the patch-context rows --auto-advance / -U 3 / --unified=3 / --inter-hunk-context=2 + the standalone bytes rows --unified=3 / --inter-hunk-context=2 / --no-auto-advance — version skew between the test runtime's git and vendor/git). Real implementations: `Add(add::Platform)` clap surface mirroring `vendor/git/builtin/add.c:253..283` (the only adoc-public flag not wired is `--no-warn-embedded-repo`, the documented inverse of the OPT_HIDDEN_BOOL at vendor/git/builtin/add.c:279, which sits in the deferred embedded-repo advisory cluster alongside the real index-update driver); porcelain placeholder in `gitoxide_core::repository::add` wiring the C-mirror precondition matrix (mirrors vendor/git/builtin/add.c:405..474): `--unified` / `--inter-hunk-context` non-negative; `--interactive` / `--patch` mutually exclusive with `--dry-run` and `--pathspec-from-file`; `-U` / `--inter-hunk-context` / `--no-auto-advance` require `--interactive` / `--patch`; `--edit` mutually exclusive with `--pathspec-from-file`; `-A` / `-u` mutually exclusive; `--ignore-missing` requires `--dry-run`; `--chmod` argument validation; `--pathspec-from-file` mutually exclusive with pathspec args; `--pathspec-file-nul` requires `--pathspec-from-file`; empty-pathspec emits "Nothing specified, nothing added." + addEmptyPathspec advice + exit 0; non-magic missing pathspec dies 128 with "pathspec '<x>' did not match any files" (mirrors vendor/git/builtin/add.c:559..568). The remaining compat_effect rows are deferred until the real add driver implements `add_files_to_cache` + `fill_directory` + `prune_directory` + `--intent-to-add` empty-blob entries + `--refresh` stat-only path + `--renormalize` clean-filter replay + `--chmod` index toggle + `--pathspec-from-file` parser + interactive add subprocess + embedded-repo advisory. sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| rm | gix rm | partial | [rm.sh](../../tests/journey/parity/rm.sh) | Scaffold: 22 sections covering the full git-rm flag surface from vendor/git/Documentation/git-rm.adoc + vendor/git/builtin/rm.c:248..262 — meta (--help/--bogus-flag/outside-of-repo), empty-pathspec die, pathspec positionals (`a`, `-- a`, `missing-file`), --ignore-unmatch, --cached, --dry-run (-n / --dry-run), --quiet (-q / --quiet), --force (-f / --force), -r, --sparse, --pathspec-from-file (alone + with --pathspec-file-nul), and precondition gates (--pathspec-file-nul without --pathspec-from-file; --pathspec-from-file with positional pathspec args). Real implementations: `Rm(rm::Platform)` clap surface mirroring `vendor/git/builtin/rm.c:248..262`; porcelain placeholder in `gitoxide_core::repository::rm` wiring the C-mirror precondition matrix (`--pathspec-from-file` mutually exclusive with positional pathspec; `--pathspec-file-nul` requires `--pathspec-from-file`; empty pathspec dies 128 with "No pathspec was given. Which files should I remove?"; non-magic missing positional pathspec dies 128 with "pathspec '<x>' did not match any files" unless --ignore-unmatch is set). Mutating rows use `expect_parity_reset _rm-fixture effect` so each binary starts from a fresh per-binary fixture (mirrors branch.sh's `_branch-move-fixture` pattern); non-mutating rows keep the cheaper `expect_parity` / `compat_effect` form. Deferred until rm driver lands: pathspec walker over the index, `not removing '<x>' recursively without -r` enforcement, check_local_mod up-to-date stanzas, `remove_file_from_index` + working-tree deletion + per-file `rm '<path>'` emission, submodule absorb, sparse-checkout advisory, parse_pathspec_file. sha256 blocker: gix-config rejects `extensions.objectFormat=sha256` (`gix/src/config/tree/sections/extensions.rs`), so every repo-opening row is `sha1-only`. Dual rows: --help, (outside a repository). |
| mv | — | absent | — | — |
| checkout | — | absent | — | — |
| switch | — | absent | — | — |
| restore | — | absent | — | — |
| branch | gix branch | present | [branch.sh](../../tests/journey/parity/branch.sh) | 37 green `it`-bearing sections covering the full git-branch flag surface from vendor/git/Documentation/git-branch.adoc OPTIONS: meta (--help, --bogus-flag, outside-repo), list mode (bare, -l/--list, -r/--remotes, -a/--all, --show-current, -v/-vv, -q/--quiet, --abbrev/--no-abbrev, --contains, --no-contains, --merged, --no-merged, --points-at, --format, --sort, --column/--no-column, --color/--no-color, --omit-empty, -i/--ignore-case), create mode (`<name>`, `<name> <start-point>`, invalid-name, existing-name, -f/--force, -t/--track + --no-track, --recurse-submodules gate-error, --create-reflog), upstream (-u/--set-upstream-to, --unset-upstream, --edit-description), move (-m/-M), copy (-c/-C with worktree-checkout guard), delete (-d/-D, -r -d). Real implementations: flag-bearing top-level Platform mirroring git's cmdmode (replaces the prior nested-Subcommand shape), pattern filter via `gix::glob::wildmatch`, ancestry filters (--contains/--no-contains via `commit_contains`, --merged/--no-merged via inclusive `ancestors_of` set), --points-at via direct ObjectId equality, minimal for-each-ref atom interpreter for --format (refname / refname:short / refname:strip=N / refname:lstrip=N + %XX hex escapes), --sort=refname / -refname (last-key wins primary), refs/remotes/ prefix when --all pairs with locals, three RefEdit-based mutating subroutines (`create` with PreviousValue::MustNotExist gate + already-exists pre-check, `rename` two-RefEdit transaction, `copy` single Update with worktree-checkout guard, `delete` with RefLog::AndReference). All flag-level compat rows in the renderer cluster (-v/-vv, --abbrev, --column=always) and the upstream-config + EDITOR-spawn cluster (-u/--set-upstream-to, --unset-upstream, --track, --edit-description) closed in the foundations sprint (etc/plan/foundations-sprint-branch-render-and-config.md): bytes-parity for all 9 rows. Foundations introduced: gix-config branch-section writers (Repository::set_branch_upstream / unset_branch_upstream / set_branch_description), shared::columns column-major packer, shared::editor::edit_file helper. Deprecated `--set-upstream` (adoc:278) intentionally omitted — git itself documents it as "no longer supported". sha256 blocker: gix-config rejects extensions.objectFormat=sha256 (gix/src/config/tree/sections/extensions.rs), so every repo-opening row is sha1-only. Dual rows: --help, (outside a repository). |
| tag | gix tag | present | [tag.sh](../../tests/journey/parity/tag.sh) | Full flag surface wired across list / delete / verify / create modes. List: -l/--list, <pattern>, -n, --sort (default order bytes; key interpreter compat-deferred), --format with %(refname[:short,:strip=N,:lstrip=N]), %(objectname), %(objecttype) + %XX hex escapes, --omit-empty, --column/--no-column (packing compat-deferred), --contains/--no-contains, --merged/--no-merged, --points-at, -i/--ignore-case, --color. Delete: -d/--delete (single, multi, mix-existing+nonexistent). Verify: -v on annotated-unsigned/lightweight/nonexistent (GPG backend still unimplemented; tests assert the three non-signature error paths). Create: lightweight `<name> [<commit>]`, -f/--force replace, invalid-name rejection (bytes), annotated `-a`/`-m` (multi-paragraph), `-F <file>` (stdin deferred pending expect_parity_reset stdin plumbing), --cleanup {verbatim/whitespace/strip/bogus}, -e/--edit (no-op under EDITOR=true), --trailer (accumulating), --create-reflog/--no-create-reflog (flag accepted; reflog write deferred at gix-ref level), -s/-u/--local-user (emit git-compat no-backend error + exit 128), --no-sign. `tag.gpgSign` config-read-then-sign is out of scope. sha256 blocker: gix-config rejects extensions.objectFormat=sha256 (gix/src/config/tree/sections/extensions.rs), so every repo-opening row is sha1-only. Dual rows: --help, (outside a repository). |
| stash | — | absent | — | — |
| cherry-pick | — | absent | — | — |
| revert | — | absent | — | — |
| bisect | — | absent | — | — |
| notes | — | absent | — | stub crate `gix-note` exists |
| worktree | gix worktree | partial | — | exists as `Worktree` subcommand |
| submodule | gix submodule | partial | — | exists as `Submodule` subcommand |
| remote | gix remote | partial | — | exists as `Remote` subcommand |
| config | gix config | partial | — | exists as `Config` subcommand |

## Plumbing (selected, not exhaustive)

| git | gix | Status | Parity file | Notes |
|---|---|---|---|---|
| cat-file | gix cat | present | [cat-file.sh](../../tests/journey/parity/cat-file.sh) | 59 green `it` sections / 72 `it` blocks across the full git-cat-file(1) flag surface: query modes (-e/-p/-t/-s), `<type> <object>` positional + type-mismatch, mailmap (compat + `--use-mailmap -s` bytes-parity via Commit/Tag signature rewrite through gix_mailmap::Snapshot::resolve_cow), textconv/filters/--path, batch family (--batch / --batch-check / --batch-command + format-atom expansion with --batch-all-objects / --buffer / --unordered / --follow-symlinks / -Z / -z), format atoms (%objectname, %objecttype, %objectsize, %objectsize:disk, %deltabase, %rest; %objectmode emits bad-format 128 in byte-parity with system git 2.47.3), combined-mode + mode+batch usage errors (129), and --allow-unknown-type. Clap surface on `Subcommands::Cat` carries `visible_alias = "cat-file"` so `gix cat-file …` routes to the existing `gix cat` canonical. --follow-symlinks uses a custom tree-walker with dir-stack + ..-escape detection for Resolved / External / Dangling status lines. Deferrals (effect-mode compat, not shortcomings): the 7 `--filter=<spec>` rows because system git 2.47.3 predates cat-file's OPT_PARSE_LIST_OBJECTS_FILTER — rows flip to bytes when the CI git catches up; the ambiguous short-sha row because git's stderr hint text varies across versions. sha1-only: `gix-config` rejects `extensions.objectFormat=sha256`; dual-hash rows (--help, --bogus-flag outside repo, outside-of-repo) pass on both. |
| rev-parse | gix revision | partial | — | — |
| ls-files | — | absent | — | — |
| ls-tree | gix tree | partial | — | — |
| verify-pack | gix free pack verify | partial | — | — |
| pack-objects | — | absent | — | — |
| pack-refs | — | absent | — | — |
| update-ref | — | absent | — | — |
| symbolic-ref | — | absent | — | — |
| hash-object | — | absent | — | — |
| mktree | — | absent | — | — |
| write-tree | — | absent | — | — |
| commit-tree | — | absent | — | — |
| read-tree | — | absent | — | — |

(This table is a seed. Complete enumeration via `etc/parity/enumerate.sh` when we're ready to scale.)

## ein — brit-native workflow tool (no git parity target)

`ein` commands are brit-native; they have no git counterpart and are **out of parity-loop scope**.

| ein command | Purpose |
|---|---|
| ein init | init repository |
| ein tool organize | consolidate git repos into a tidy directory tree |
| ein tool find | locate git repos recursively |
| ein tool estimate-hours | estimate contributor time from commit history |
| ein tool query | object-graph queries |
