# docs/plans — brit EPR vision roadmap

This directory holds the brit-native protocol roadmap (covenant trailers, ContentNodes, DHT discovery, fork governance). **Paused pending git↔gix parity.** Phases 0+1 and 2a are complete; phases 2–6 are summary-only.

If you're editing code under this directory's scope, you're on `main` (or a feature branch off it). If you're on `gix-brit`, this directory should not be your target — gix parity work belongs in the workspace crates, not in docs/plans.

See root `CLAUDE.md` for branch discipline.
