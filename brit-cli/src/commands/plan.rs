//! brit plan — stub. Implementation in Task 17.

use crate::error::Result;

pub fn run(_repo: &std::path::Path, _files: Option<&str>, _since: Option<&str>, _pipeline: Option<&str>) -> Result<()> {
    eprintln!("brit plan: not yet implemented");
    Ok(())
}
