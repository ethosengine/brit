//! brit affected — stub. Implementation in Task 16.

use crate::error::Result;

pub fn run(_repo: &std::path::Path, _files: Option<&str>, _since: Option<&str>) -> Result<()> {
    eprintln!("brit affected: not yet implemented");
    Ok(())
}
